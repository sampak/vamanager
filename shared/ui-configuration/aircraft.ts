export interface AircraftUIConfiguration {
  canChangeImage?: boolean;
  canSellAircraft?: boolean;
}

export enum AircraftButtons {
  canChangeImage = "canChangeImage",
  canSellAircraft = "canSellAircraft",
}
